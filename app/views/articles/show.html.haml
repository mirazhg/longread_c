= link_to 'Destroy', article_path(@article), method: :delete, data: { confirm: 'Are you sure?' }


%p
  %strong
    = @article.caption
%br
  %i
    = @article.lead

- @article.fields.each do |field|
  = render "fields/#{field.teg}", field: field
-#= render @article.fields
= render 'fields/form'

