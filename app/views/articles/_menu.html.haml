- if can? :update, article
  .b-admin.b-admin_menuarticle
    = link_to 'Destroy', article_path(article), method: :delete, data: { confirm: 'Are you sure?' }
    = link_to 'Edit', edit_article_path(article)
